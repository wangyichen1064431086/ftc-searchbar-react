//import SearchBar from '../../src/js/SearchBar.js';
import SearchBar from '../../build/index.es';
import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <SearchBar postUrl="#" sticky={true}/>,
  document.getElementById('root')
);